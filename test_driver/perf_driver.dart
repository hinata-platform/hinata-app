// Driver for integration_test performance runs — writes the collected
// FrameTiming summaries to build/integration_response_data.json.
import 'package:integration_test/integration_test_driver.dart';

Future<void> main() => integrationDriver();
